export const CONFIG = {
    // ⚠️ สำคัญ: 
    // - หากใช้ Android Emulator ให้ใช้: 10.0.2.2
    // - หากใช้ iOS Simulator ให้ใช้: localhost
    // - หากใช้มือถือเครื่องจริง ให้ใช้: IP ของคอมพิวเตอร์ (เช่น 192.168.1.xx)
    API_URL: 'http://localhost:3000',
    WS_URL: 'ws://localhost:3000/ws',
};